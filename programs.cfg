{
  path="/usr",
  repos={
    ["Cyakat/emailclient-server"] = {
      ["client-package"] = {
        files = {
          ["master/lua-files/emailclient.lua"] = "/bin",
          ["master/lua-files/client-lib/messageClassClient.lua"] = "/lib"
        },
        name = "emailclient",
        description = "This is a client for a simple encrypted email program",
        authors = "Cyakat",
        note = "Sadly there is no end-to-end encryption due to a bug that prevents keys written in files from being deserialized properly"
        hidden = false,
        repo="tree/master/lua-files"
      },
      ["server-package"] = {
        files = {
          ["master/lua-files/emailserver.lua"] = "/bin",
          ["master/lua-files/server-lib/messageClassServer.lua"] = "/lib"
        },
        name = "emailserver",
        description = "This is a server for a simple encrypted email program",
        authors = "Cyakat",
        note = "Sadly there is no end-to-end encryption due to a bug that prevents keys written in files from being deserialized properly"
        hidden = false,
        repo="tree/master/lua-files"
      }
    }
  }
}
